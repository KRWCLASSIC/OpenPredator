namespace OpenPredator.Core.Enums;

public enum FanMode
{
    Auto = 0,
    Max = 1,
    Custom = 2
}

public enum PowerMode
{
    Quiet = 0,
    Default = 1,      // Balanced
    Performance = 2,
    Turbo = 3         // Extreme
}

public enum RgbEffectType
{
    Static = 0,
    Breathing = 1,
    Neon = 2,
    Wave = 3,
    Zoom = 4,
    Shifting = 5
}
